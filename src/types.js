/**
 * @typedef {(t:number)=>number} Ease
 * @typedef {HTMLElement|SVGElement|Window|Document} Target
 * @typedef {{ [prop:string]: any }} Dict
 * @typedef {{ from: any, to: any, unit?: string, prop: string, ease: Ease }} Tween
 * @typedef {{ target: Target, tweens: Tween[] }} RenderItem
 */

export {};


